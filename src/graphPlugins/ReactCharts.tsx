import * as React from "react";
import { GraphItems } from "../plugins/GraphPlugins";

export const GraphWithReactCharts: GraphItems = function GraphWithReactCharts(
  values
) {
  return <>react-charts support is not yet implemented!</>;
};
