import * as React from "react";
import { GraphItems } from "../plugins/GraphPlugins";

export const GraphWithChartJs: GraphItems = function GraphWithReactCharts(
  values
) {
  return <>Chart.js support is not yet implemented!</>;
};
